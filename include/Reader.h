#ifndef READER_H
#define READER_H
/// @brief Uses a third-party library to parse the yaml files and get the needed values.
/// @param utilizacao its the current targeted utilization of the processor.

int reader(int utilizacao);

#endif